name: BMS Continuous Integration

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: "npm"

    - name: Install Dependencies
      run: npm ci

    - name: Run Tests
      run: npm test

    - name: Run Build
      run: npm run build
